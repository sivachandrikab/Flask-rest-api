# Flask-rest-api
 A simple Flask-based REST API for managing user data. Supports CRUD operations (Create, Read, Update, Delete) with JSON responses and proper status codes. User data is stored in memory for testing. Demonstrates REST fundamentals, HTTP methods, and Flask routing. 
